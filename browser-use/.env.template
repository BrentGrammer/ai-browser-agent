# For the unsandboxed Browser-Use experiment ONLY — not the base for the root .env.
# Copy to browser-use/.env; use a throwaway login, never real credentials.
OPENAI_API_KEY=
LOGIN_USERNAME=
LOGIN_PASSWORD=
TARGET_URL=
