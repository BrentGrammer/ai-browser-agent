# For the unsandboxed Browser-Use experiment ONLY — not the base for the root .env.
# Copy to .env in this folder. These credentials are sent to the LLM in the prompt,
# so use a dedicated test account with a unique password — never a real account.
OPENAI_API_KEY=
LOGIN_USERNAME=
LOGIN_PASSWORD=
TARGET_URL=
