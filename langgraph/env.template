OPENAI_API_KEY=
LOGIN_USERNAME=
LOGIN_PASSWORD=
TARGET_URL=
# Optional: extra domains the agent may navigate to (comma-separated).
# The TARGET_URL host is always allowed.
ALLOWED_NAV_DOMAINS=
# Optional overrides (Docker sets these in docker-compose.yml):
# HEADLESS=true
# BROWSER_PROXY=
# USER_DATA_DIR=
# SCREENSHOT_DIR=
# MEMORY_FILE=
